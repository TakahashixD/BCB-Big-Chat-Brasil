server:
  port: 8100
spring:
  application:
    name: bcb-sms-service
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  config:
    import:
    - optional:configserver:http://localhost:8888/
#  cloud:
#    config:
#      profile: default
