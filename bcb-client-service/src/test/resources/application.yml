server:
  port: 8080
spring:
  application:
    name: bcb-client-service
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
      show-sql: false
  config:
    import:
    - optional:configserver:http://localhost:8888/
#  cloud:
#    config:
#      profile: default
  flyway:
    url: jdbc:postgresql://localhost:5432/
    schemas: bcb
    user: bcb
    password: 123456
